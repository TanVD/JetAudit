out
**/*.vpp.bak*
**/*.vpp.lck*
.gradle
/build/

# Cause we now use gradle
.idea
*.iml

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache
